<div class='container p-3'>
    <div class='row border border-dark bg-light'>
        <div class='col bg-secondary p-4'>
        <h1>My Dashboard</h1>
        </div>
    </div>
</div>

{{#if blogs.length}}
{{#each blogs as |blog| }}
<div class='container p-3'>
    <div class='row border border-dark bg-light'>
        <div class='col bg-secondary text-white p-2' id='blog-title'><a href='/blog/{{blog.id}}'>{{blog.title}}</a></div>
    </div>
</div>
{{/each}}
{{/if}}